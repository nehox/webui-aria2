# Dockerfile pour build uniquement
FROM node:14-alpine

WORKDIR /app

# Copier les fichiers de configuration
COPY package*.json ./

# Installer les dépendances
RUN npm install

# Copier le code source
COPY . .

# Build l'application
RUN npm run build

# Les fichiers compilés seront dans le dossier docs/
